# Optional local static host port (production uses Railway PORT)
# PORT=8080

# Frontend build (set at Docker build time in production)
# VITE_API_URL=https://api.connectwithwago.com

# Local Vite → WAIGO proxy target (frontend/vite.config.ts)
# VITE_DEV_API_PROXY=http://localhost:3001
