{
  "name": "rsm-tools",
  "version": "2.0.0",
  "private": true,
  "description": "RSM Tools SPA — static host; APIs via shared WAIGO backend",
  "main": "server.js",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "node server.js",
    "preview": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
